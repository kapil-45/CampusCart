# Known-compatible version combination for Djongo + MongoDB.
# Djongo has NOT kept pace with modern Django — using newer versions of
# any of these together (especially Django 4.x/5.x) will break with cryptic
# SQL-parsing errors, since Djongo works by translating Django's SQL-style
# ORM queries into MongoDB queries under the hood.
#
# Install with: pip install -r requirements.txt

Django==3.1.14
djongo==1.3.6
djangorestframework==3.12.4
djangorestframework-simplejwt==4.8.0
pymongo==3.12.3
sqlparse==0.2.4
django-cors-headers==3.10.1
